isPrime(n): 
  for(i = 2, i*i <= n; i++) {      <-- 2^k 
    if(n % x == 0) {
      return false
    }
  return true

Let i*i = n, i^2 = n, i = sqrt(n)

1. Best Case: Even number, so O(1) 
2. Worst Case: Number is prime, so O(sqrt(n))
3. Average Case: O(sqrt(n))

Time complexity: O(sqrt(n))
